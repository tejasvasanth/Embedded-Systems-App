import { moderateScale } from './responsive';

export const spacing = {
  xs: moderateScale(4),
  sm: moderateScale(8),
  md: moderateScale(14),
  lg: moderateScale(20),
  xl: moderateScale(26),
  xxl: moderateScale(36),
};
